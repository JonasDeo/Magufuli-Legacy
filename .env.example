PORT=4000
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=replace-with-strong-secret
CAPTCHA_SECRET=replace-with-captcha-secret

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/magufuli

# App URL (used in password-reset emails)
APP_URL=http://localhost:5173

# SMTP (optional in dev — reset link is logged to console if unset)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@magufuli.local
